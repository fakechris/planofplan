{
  "name": "planofplan",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local dashboard for tracking usage/quota across AI coding-plan subscriptions",
  "scripts": {
    "serve": "bun src/cli.ts serve",
    "menubar:build": "./scripts/build-menubar.sh",
    "demo": "bun src/cli.ts serve --demo",
    "usage": "bun src/cli.ts usage",
    "status": "bun src/cli.ts status",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@steipete/sweet-cookie": "0.4.1",
    "hono": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
